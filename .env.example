# Persistence mode:
# - json: JSON runtime store only (current default)
# - hybrid: PostgreSQL for migrated domains + JSON for remaining domains
# - postgres: alias for hybrid in this transition phase
SKOSS_PERSISTENCE_MODE=hybrid

# Required when SKOSS_PERSISTENCE_MODE is hybrid/postgres
DATABASE_URL=postgresql://skoss:skoss@localhost:5432/skoss
